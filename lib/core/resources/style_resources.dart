import 'package:flutter/material.dart';

class StyleResorces {
  StyleResorces._();

  static const scaffoldBackgroundColor = Color(0xFF363740);
  static const accentColor = Colors.white;
  static const shadedText = Colors.grey;
  static const primaryColor = Color(0xFF3751FF);
}
